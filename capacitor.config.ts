import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'api_Rick-Morty',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
